{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PlaceMatch#source_fields",
  "description": "Source fields for a PlaceMatch model",

  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the location scrobble being matched",
      "type": ["string", "null"]
    },

    "latitude": {
      "description": "The latitude of the location scrobble being matched",
      "type": ["string", "number"]
    },

    "longitude": {
      "description": "The longitude of the location scrobble being matched",
      "type": ["string", "number"]
    }
  },
  "required": ["latitude", "longitude"]
}
